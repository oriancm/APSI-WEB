<?php
/**
 * APSI-WEB Image Optimizer Utility (Disabled)
 * Automatic compression and card generation has been disabled at user request.
 * All uploads are now saved exactly as uploaded.
 */

function optimizeUploadedImage($filePath) {
    // Disabled - do nothing and return true to keep uploaded images exactly as is.
    return true;
}
